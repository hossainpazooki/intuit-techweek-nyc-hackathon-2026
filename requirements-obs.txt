# Observability + dataset-exploration layer (on top of the base numpy/pandas in
# requirements.txt). Kept dependency-light: a fast columnar engine, the stats
# needed for drift, and one viz/UI stack.
polars>=1.0
pyarrow>=15.0
numpy>=1.26
pandas>=2.2
scipy>=1.11
plotly>=5.20
streamlit>=1.30
pytest>=8.0
